# Comma-separated list of origins allowed to connect via CORS
# This should be the URL of your PHP frontend (from tiny/.env APP_URL)
ALLOWED_ORIGINS="http://localhost:8010"

# This secret key MUST match the PYTHON_SECRET_KEY in your python/.env file
PYTHON_SECRET_KEY="your-long-random-secret-key-here"

# The port for this Node.js server to run on
PORT=3000