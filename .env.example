# Comma-separated list of origins allowed to connect via CORS
# This should be the URL of your PHP app on its shared host
ALLOWED_ORIGINS="https_://www.your-php-domain.com"

# This secret key is used to authorize key generation from the PHP admin.
# It MUST match the NODE_ADMIN_SECRET in the PHP app's .env
ADMIN_SECRET_KEY="your-super-long-admin-secret-key"

# The port for this Node.js server to run on (Render sets this automatically)
PORT=3000